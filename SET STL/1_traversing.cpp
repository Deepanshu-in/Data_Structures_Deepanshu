#include <bits/stdc++.h>
using namespace std;


int main()
{
    set<int> s;
    s.insert(40);
    s.insert(83);
    s.insert(1);
    s.insert(30);
    for(int x:s){
        cout<<x<<" ";
    }
return 0;
}